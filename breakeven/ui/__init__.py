"""The operator UI — F-UI-01, F-UI-04, F-UI-05.

`events.py` records what the agents do; `app.py` streams it to a browser over one
WebSocket. Nothing in this package decides anything: it observes the modules that do.
"""
